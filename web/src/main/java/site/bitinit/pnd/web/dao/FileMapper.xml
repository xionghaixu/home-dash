<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.bitinit.pnd.web.dao.FileMapper">

    <sql id="fileFields">
        ${alias}.id, ${alias}.file_name, ${alias}.parent_id, ${alias}.type, ${alias}.resource_id, ${alias}.create_time, ${alias}.update_time
    </sql>
    <sql id="sizeField">
        ${alias}.size
    </sql>

    <select id="findById" resultType="file">
        SELECT
          <include refid="fileFields"><property name="alias" value="f" /></include>,
          <include refid="sizeField"><property name="alias" value="r" /></include>
        FROM file f
        LEFT JOIN resource r ON f.resource_id = r.id
        WHERE f.id = #{id}
    </select>

    <select id="findByIds" resultType="file">
        SELECT
          <include refid="fileFields"><property name="alias" value="f" /></include>,
          <include refid="sizeField"><property name="alias" value="r" /></include>
        FROM file f
        LEFT JOIN resource r ON f.resource_id = r.id
        WHERE f.id IN
        <foreach collection="ids" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="findAll" resultType="file">
        SELECT
          <include refid="fileFields"><property name="alias" value="f" /></include>,
          <include refid="sizeField"><property name="alias" value="r" /></include>
        FROM file f
        LEFT JOIN resource r ON f.resource_id = r.id
        ORDER BY
        <choose>
            <when test="sortBy == 'size'">
                r.size
            </when>
            <when test="sortBy == 'createTime'">
                f.create_time
            </when>
            <when test="sortBy == 'updateTime'">
                f.update_time
            </when>
            <otherwise>
                f.file_name
            </otherwise>
        </choose>
        <if test="sortOrder == 'desc'"> DESC</if>
        <if test="sortOrder != 'desc'"> ASC</if>
    </select>

    <select id="findRecentFiles" resultType="file">
        SELECT
          <include refid="fileFields"><property name="alias" value="f" /></include>,
          <include refid="sizeField"><property name="alias" value="r" /></include>
        FROM file f
        LEFT JOIN resource r ON f.resource_id = r.id
        WHERE f.type != 'folder'
        ORDER BY f.create_time DESC, f.id DESC
        LIMIT #{limit}
    </select>

    <select id="findByParentId" resultType="file">
        SELECT
          <include refid="fileFields"><property name="alias" value="f" /></include>,
          <include refid="sizeField"><property name="alias" value="r" /></include>
        FROM file f LEFT JOIN resource r ON f.resource_id = r.id
        WHERE f.parent_id = #{parentId}
        <if test="sort">
            ORDER BY f.id
        </if>
        <if test="!sort and sortBy != null and sortOrder != null">
            <choose>
                <when test="sortBy == 'size'">
                    ORDER BY r.size
                    <if test="sortOrder == 'desc'">DESC</if>
                    <if test="sortOrder != 'desc'">ASC</if>
                </when>
                <when test="sortBy == 'updateTime'">
                    ORDER BY f.update_time
                    <if test="sortOrder == 'desc'">DESC</if>
                    <if test="sortOrder != 'desc'">ASC</if>
                </when>
                <otherwise>
                    ORDER BY f.file_name
                    <if test="sortOrder == 'desc'">DESC</if>
                    <if test="sortOrder != 'desc'">ASC</if>
                </otherwise>
            </choose>
        </if>
    </select>

    <select id="findByParentIdForUpdate" resultType="file">
        SELECT
          <include refid="fileFields"><property name="alias" value="f" /></include>
        FROM file f
        WHERE  f.parent_id = #{parentId}
        FOR UPDATE
    </select>

    <select id="countByParentIdAndFileName" resultType="int">
        SELECT COUNT(1)
        FROM file
        WHERE parent_id = #{parentId}
          AND file_name = #{fileName}
        <if test="excludeId != null">
          AND id != #{excludeId}
        </if>
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id" parameterType="file">
        INSERT INTO
          file (file_name, parent_id, type, resource_id, create_time, update_time)
        VALUES (#{fileName}, #{parentId}, #{type}, #{resourceId, jdbcType=BIGINT}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="file">
        UPDATE file
        SET
        <if test="parentId != null">
            parent_id = #{parentId},
        </if>
        <if test="fileName != null">
            file_name = #{fileName},
        </if>
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <update id="updateParentId">
        UPDATE file
        <set>
            parent_id = #{parentId},
            update_time = #{updateTime}
        </set>
        WHERE id IN
        <foreach collection="ids" index="index" open="(" close=")" item="item" separator=",">
            #{item}
        </foreach>
    </update>
    
    <delete id="deleteByIds" parameterType="long">
        DELETE FROM file
        WHERE 1 > 2 OR id IN
        <foreach collection="ids" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <select id="getAllFileType" resultType="file">
        SELECT id, type FROM file
    </select>
</mapper>
